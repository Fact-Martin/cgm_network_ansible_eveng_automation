---
- name: Deploy Standardized Network Architecture Templates
  hosts: cgm_network
  gather_facts: false

  tasks:
    - name: Render and push the baseline configuration profile to Cisco nodes
      cisco.ios.ios_config:
        src: templates/cgm_network_base_config.j2
        save_when: changed

